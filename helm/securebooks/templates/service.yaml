apiVersion: v1
kind: Service
metadata:
  name: {{ include "securebooks.fullname" . }}
  labels:
    {{- include "securebooks.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "securebooks.selectorLabels" . | nindent 4 }}
